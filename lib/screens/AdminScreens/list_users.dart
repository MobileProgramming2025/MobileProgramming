import 'package:flutter/material.dart';

class ListUsersScreen extends StatelessWidget {
  const ListUsersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("List users screen");    
  }
}